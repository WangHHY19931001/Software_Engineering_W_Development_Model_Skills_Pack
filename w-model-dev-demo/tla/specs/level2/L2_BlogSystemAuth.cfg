SPECIFICATION Spec
INVARIANTS
    TypeInvariant
    PasswordHashedWhenRegistered
    AccessRequiresValidToken
    TokenValidRequiresLogin
    ProfileRequiresAuthContext

(* 无常量：L2 auth 取值域在 .tla 内联定义 *)
