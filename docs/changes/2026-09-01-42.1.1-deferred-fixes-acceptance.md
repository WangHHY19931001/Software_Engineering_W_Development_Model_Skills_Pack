# 42.1.1 deferred 项修正验收记录

> 收口范围：`e20e579`+`0d03b4a`（竞态修复）、`7e97baa`（baseline 对账）、`93f6f3a`（活体文档微修）3 个已落库 commit 的发布收口（版本/CHANGELOG/本记录），另含 1 个经用户裁定追加的存量门禁测试修复 commit `ef6d203`（见「偏差登记」）。设计见 docs/superpowers/specs/2026-09-01-w-model-dev-full-optimization-design.md §4。

## 门禁矩阵（真实退出码）

| 命令 | 结果 |
|---|---|
| `npm run version:bump -- 42.1.1` | exit 0，7 文件同步（package.json / skill-metadata.json / SKILL.md frontmatter / README 当前版本 / INSTALL 激活 YAML / package-lock 两处 / CHANGELOG 节头 `## [42.1.1] - 2026-09-01`） |
| `npm run self-test` | exit 0，262/262 |
| run-sync × dependency-boundaries 组合 5 连跑 | 5/5 OK（RUN 1-5 全 OK，0 FAILED） |
| `npx tsx w-model-dev/scripts/cli/check-docs-consistency.ts` | exit 0（静态违规 0、动态违规 0；内嵌 vitest 探针全量 1246 用例 0 失败，`success:true`） |

> 门禁执行环境说明：docs-consistency 的动态 vitest 探针会以 `execFile('bash', ...)` 执行 `.githooks/*.sh` 回归（pre-push 17 项门禁三态样本），本机按 docs/INSTALL.md Windows 边界（「Bash 只用于 pre-push 和平台依赖检查…必须在 Bash（Git Bash / WSL / POSIX shell）中运行」）在 Git Bash PATH 前置会话（`$env:Path = "C:\Program Files\Git\bin;..."`）下执行，避免默认 PATH 命中 WSL bash（`C:\Windows\system32\bash.exe`）导致 Windows 路径脚本解析失败（exit 127）与平台探测漂移（linux-x64 误报）。

## baseline 对账终值

`.eslintsecurity-baseline.json` 411 → 295 条（删 116 孤儿；实测 `entries.length`）。

## 偏差登记

- **批次 1 原定「零代码变更」，实际追加 1 个测试修复 commit `ef6d203`**（用户裁定「修复后收口」）：
  - **根因**：`docs-consistency-logic.test.ts` 的 `assertPrePushArtifactCleanup` 原以「PATH 前置假可执行文件（npm/npx/mktemp）」劫持 pre-push 的 `mktemp -d`；Git Bash/MSYS 启动时会自动把 `/mingw64/bin:/usr/bin:...` 前置到转换后的 PATH（`type -a mktemp` 实测假件排第 3），PATH 前置劫持被 `/usr/bin/mktemp` 压制而失效——受控 artifact 目录断言拿到真 mktemp 产物（`tmp.XXXXXXXXXX`）而失败。
  - **修复方案**：迁移为仓内先例 `platform-deps-hook.test.ts run()` 的 BASH_ENV 函数导出模式（`bash -c 'source "$BASH_ENV"; export -f npm npx mktemp ...; bash "$script" "$@"'`）——bash 函数优先于 PATH 查找，不受 MSYS 默认 PATH 前缀影响；假 mktemp 语义不变（`-d` 输出受控 `$WM_PREPUSH_ARTIFACT_DIR`，其余转发 `/usr/bin/mktemp`），独立脚本版 `exit N` 对应函数版 `return N`，断言语义与被测行为零变更，仅改测试文件。
  - **存量性证据**：干净 HEAD `9382fdd`（stash 掉本收口全部改动）复现同一失败；与 42.1.1 三个被收口 commit 及版本号变更无关（run-sync × dependency-boundaries 竞态组合在修复前后均 5/5 全绿）。
  - 修复验证：定向 1 passed | 151 skipped → 单文件 152/152 → docs-consistency exit 0（动态探针 1246/1246）。

## 未执行命令不作通过声明

- `npm run prepush`、`npm run eval`、`check-samples-coverage` 等本任务简报未列命令均未在本记录中执行/声明；run-sync × dependency-boundaries 5 连跑在测试修复 commit 前验证（该修复仅改 docs-consistency-logic.test.ts，与两测试文件无交集，无重跑必要）。
