# 42.2.0 易用性清债验收记录

> 范围：19 个过期重定向 stub 移除 + 13 处活体引用重链 + SKILL.md 收敛 <100 非空行。设计见 docs/superpowers/specs/2026-09-01-w-model-dev-full-optimization-design.md §5。

## stub 承诺过期闭环说明

19 个 stub 的重定向注记写明「将于 42.1.0 移除」，42.1.0 版本号被证据支撑树集成（evidenceAnchor）使用，清债顺延至 42.2.0——本记录即该承诺的正式闭环。

## 变更清单

- 重链 13 处：tools/README ×2（tla-plus-guide→tla-plus）、examples ×3（dispatch-matrix→subagent-delegation）、schemas ×8（tla-manifest ×2 / code-tla-manifest ×3 / hill-climbing-report ×1 / iceberg-sweep ×2）。
- 删除 19 个 stub（2 行重定向文件，逐一确认后删除）；references 59 → 40。
- 不改项登记：coding-quality.md 合并史实注记、quick-self-check.md 外部出处、phase/templates 全部 glossary.md（模板子文件）、scripts 内 dispatch-matrix 节名引用（章节语义）、全部历史文档。
- SKILL.md 106 → 99 非空行（7 处剪辑，见 commit）。

## 门禁矩阵（真实退出码）

| 命令 | 结果 |
|---|---|
| `npm run eval` | exit 0，25/25 |
| `npm run self-test` | exit 0，262/262 |
| `check-docs-consistency.ts` | exit 0；静态违规 0、动态违规 0；vitest 1,246/1,246 |
| `check-samples-coverage.ts` | exit 0，真实 `fixtureCount=282`、`referencedFiles=244`、`referencedDirs=15`、`unregistered=0`、`undeclaredDirs=0`（简报预期为 280 fixtures；以本次真实 JSON 为准） |
| `npm run prepush`（Git Bash） | exit 0，17 项全绿；self-test、验证器/门禁样本、安全扫描、BDD、coverage、豁免、签名链、vitest+coverage、npm audit、docs-consistency、samples coverage、Prettier、tsc 均通过 |

## 版本同步文件

- `package.json`
- `package-lock.json`
- `w-model-dev/skill-metadata.json`
- `w-model-dev/SKILL.md` frontmatter
- `README.md` 当前版本
- `docs/INSTALL.md` 激活 YAML
- `CHANGELOG.md` 42.2.0 节头与正文

## commit 身份链

本次易用性清债与发布收口的实际提交链如下。

- `1cf2a6f` `refactor(references): re-link 13 live stub references to merged targets (pre-stub-removal)`
- `4c463e9` `refactor(references): remove 19 overdue redirect stubs (59 -> 40 md files)`
- `7b4cd80` `refactor(skill): compress SKILL.md 106 -> 99 non-empty lines (merge quick-check footer, workflow steps, trigger rows, checkpoint quotes)`
- 本 release commit：`9f13ace911f8d52a8b5dac215b5ce20c1f5b3396` — `release(42.2.0): usability debt cleanup (19 stubs removed, SKILL.md <100 lines, gates green)`。

## 偏差与疑虑

- Task 5 简报及其占位正文预期 samples-coverage 为 280 fixtures；本次命令真实输出为 282 fixtures、244 个引用文件、15 个引用目录，且 `unregistered=0`、`undeclaredDirs=0`。CHANGELOG 与本记录均以该真实测量值登记。
- `npm install` 在隔离 worktree 执行成功；产生的 `node_modules/` 为忽略的本地生成物，不提交。
- `npm run prepush` 使用 Git Bash PATH/入口执行，未实际 push。
