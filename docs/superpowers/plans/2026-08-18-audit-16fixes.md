# W-Model 技能包审计 16 项修复执行计划（2026-08-18）

> **For agentic workers:** 串行子代理执行（每任务派发一个子代理，禁止并行修改）。批次间隔由主会话跑全量验证。
> 步骤用 checkbox 跟踪。设计依据：`docs/superpowers/specs/2026-08-18-audit-16fixes-design.md`（决策与勘误权威源 D1-D5 / E1-E3）。
> 步骤底稿：`docs/superpowers/specs/2026-08-16-audit-16fixes-plan.md`（Task 1-12 TDD 步骤、代码片段、验证命令，零占位符）。

**Goal:** 修复 2026-08-16 审计发现的 16 项问题（P1-P16），应用 spec 勘误 E1-E3，全量回归通过，版本升至 41.18.0。

**Architecture:** 4 批次串行推进——批 1 代码缺陷（Task 1-4）→ 批 2 结构调整（Task 5-6）→ 批 3 内容修正（Task 7-11）→ 批 4 发布收尾（Task 12）。每批末跑全量验证门禁，通过才进下一批。任务步骤以底稿为权威源，本计划只附加编排指令与勘误覆盖。

**Tech Stack:** TypeScript (tsx runtime) / vitest / JSON Schema draft-07 / Node 标准库（node:fs / node:crypto / node:child_process）。

---

## 0. 全局约定（每个子代理任务书必含，主会话派发时复制本节）

1. **仓库根**：`REPO = d:\w_skill_opt\Software_Engineering_W_Development_Model_Skills_Pack`。所有命令在 REPO 下执行（package.json 所在目录）。**底稿中所有 `/workspace` 路径一律替换为 REPO**。
2. **底稿引用规则**：执行底稿（16fixes-plan.md）中指定 Task 的 Step 1..M。本计划「勘误覆盖」小节的内容**优先于底稿原文**；其余步骤严格按底稿执行（底稿代码片段可直接采用，但先读现场文件核对行号与签名，行号漂移时以内容定位）。
3. **禁止**：引入新依赖；修改 `eval/`、`docs/superpowers/` 既有文件、`.trae-html-share-packages/`；并行修改文件。
4. **TDD 纪律**（代码类任务）：先写失败测试 → 跑确认 FAIL → 实现 → 跑确认 PASS → self-test → commit。
5. **文档五步法**（文档类任务）：grep 定位 → 读取上下文 → 分析 → 按勘误终态修改 → 重读确认。
6. **收尾报告**：改动文件清单 + 每条验证命令及其输出摘要（含退出码）。报告不通过项时禁止自行扩大修复范围，回报主会话决策。
7. **Commit**：conventional 风格，message 用底稿各任务给定的文案；勘误覆盖不改变 message。

---

## 批 1：代码缺陷修复（Task 1-4，P2/P4/P3/P10）

### Task 1（P2）：run-log action 枚举同步 + docs-consistency 语义化强化

**子代理任务书**
- 必读：底稿 `### Task 1` 全节（Step 1-7）；现场文件 `w-model-dev/scripts/logic/docs-consistency-logic.ts`（checkRunLogAction 真实签名）、`w-model-dev/references/data-models.md:391/:558`
- 执行：底稿 Step 1-7 依次完成。Step 1 测试代码底稿已给全文；若 checkRunLogAction 非导出或签名不同，按底稿注意事项经 `runDocChecks` 等入口构造，断言语义不变（缺值/多值必须产生含「漂移」的 violation）
- 勘误覆盖：无
- 验证（底稿 Step 6）：`npx vitest run --config config/vitest.config.ts __tests__/docs-consistency-logic.test.ts` 全绿；`npx tsx w-model-dev/scripts/cli/check-docs-consistency.ts` passed:true；`npm run self-test` 256/256
- Commit：`fix: 同步 run-log action 枚举至 27 值并强化 docs-consistency 语义比对（P2）`

### Task 2（P4）：wm-write 原子写修复

**子代理任务书**
- 必读：底稿 `### Task 2` 全节（Step 1-5）；现场 `w-model-dev/scripts/logic/state-write-logic.ts`（100-135 行区域）
- 执行：底稿 Step 1-5。tmpPath 改 `tmp-${process.pid}-${randomUUID()}`；回读失败自动回滚（有备份 copyFile 回滚 / 无备份 unlink），`StateWriteResult` 增 `rolledBack`，`StateWriteOptions` 增 `readbackImpl` 测试缝；同步文件头注释
- 勘误覆盖：无
- 验证：`npx vitest run --config config/vitest.config.ts __tests__/state-write-logic.test.ts` 全绿；`npm run self-test` 256/256
- Commit：`fix: wm-write tmpPath 唯一化 + 回读失败自动回滚备份（P4）`

### Task 3（P3/P15）：TLA+ 门禁超时 + EXEC_LIMITS + Java 版本单源

**子代理任务书**
- 必读：底稿 `### Task 3` 全节（Step 1-7）；现场 `w-model-dev/scripts/cli/check-tla-model.ts`（103-118/181-251/290-308/427-432 行区域）、`w-model-dev/scripts/logic/doctor-logic.ts`
- 执行：底稿 Step 1-7。新建 `lib/constants.ts` 的 EXEC_LIMITS 段、`lib/java-version.ts`、`__tests__/java-version.test.ts`；SANY/TLC 加 timeout + 超时标记；Java 预检去硬编码 11；doctor-logic 切换到 lib 单源（若与 lib 版本有行为差异，以 doctor-logic 现行为准合并进 lib，两侧测试都过）
- 勘误覆盖：无
- 验证（底稿 Step 6）：vitest 三个测试文件全绿；`npm run self-test` 256/256；check-tla-model 对 samples/tla/valid.json --phase=1 按环境得 exit 0 或完整 ERROR_JSON 的 exit 2
- Commit：`fix: SANY/TLC 执行超时 + Java 版本解析单源化（P3/P15）`

### Task 4（P10）：错误出口统一（HandledCliError + runMain）

**子代理任务书**
- 必读：底稿 `### Task 4` 全节（Step 1-6）；现场 `w-model-dev/scripts/lib/cli-error.ts`、`lib/read-json-or-exit.ts`、`cli/` 目录清单
- 执行：底稿 Step 1-6。新建 `lib/run-main.ts` + 测试；read-json-or-exit 哨兵化；全仓 cli/*.ts catch 段迁移为 `runMain(main)`（plan-chunks.ts 留待 Task 6）。底稿 Step 4 的 grep 核对在 PowerShell 下改用 Grep 工具：pattern `category: 'UNEXPECTED'` 范围 `w-model-dev/scripts/cli/`，期望仅剩 main 内部的合法调用、无 catch 样板残留
- 勘误覆盖：无
- 验证（底稿 Step 5）：vitest 三个测试文件全绿；`npm run self-test` 256/256；`npx tsx w-model-dev/scripts/cli/check-verifier-output.ts <不存在的路径>` 得 exit=2、stderr 一行 ✗、stdout 恰一条 ERROR_JSON
- Commit：`refactor: 错误出口统一 HandledCliError + runMain，消除 ERROR_JSON 截断/双打印风险（P9/P10）`

- [ ] **批 1 末主会话验证门禁**（全部通过才进批 2；记录输出摘要留档）

```bash
# 在 REPO 下依次执行，期望全部通过
npm run self-test                                          # 256/256，exit 0
npx vitest run --config config/vitest.config.ts            # 全绿（42+ files / 691+ tests，记录实测数）
npx tsx w-model-dev/scripts/cli/check-docs-consistency.ts  # passed:true
```

---

## 批 2：结构调整（Task 5-6，P5/P7）

### Task 5（P5a）：schema-loader 分层修复

**子代理任务书**
- 必读：底稿 `### Task 5` 全节（Step 1-5）；现场 `w-model-dev/scripts/logic/schema-loader.ts`（约 70 行）及其导出使用点（grep `schema-loader` 排除 __tests__）
- 执行：底稿 Step 1-5。新建 `lib/schema-fs.ts`（readSchemasDir）；schema-loader 去 `node:fs` / `process.exit` / 手拼 ERROR_JSON，ajv 缺失改抛 Error；调用方（gate-logic.ts、lib/load-and-validate.ts 等）适配；gate-logic 保持 `__tests__/README.md` 既有豁免
- 勘误覆盖：无
- 验证（底稿 Step 4）：`__tests__/schema-validation.test.ts` 全绿；`npm run self-test` 256/256；纯度 grep（Grep 工具 pattern `from 'node:fs'|process\.(exit|argv|env|stdout|stderr)` 范围 `w-model-dev/scripts/logic/`）期望仅 gate-logic.ts 命中
- Commit：`refactor: schema-loader 去 exit/IO 下沉 lib，恢复 logic 层纯度（P5）`

### Task 6（P5b/P7）：plan-chunks 拆分 + 计数口径统一【含 E2/E3 勘误】

**子代理任务书**
- 必读：底稿 `### Task 6` 全节（Step 1-5）；现场 `w-model-dev/scripts/logic/plan-chunks.ts`（293 行）、`SKILL.md:165`、`docs/INSTALL.md:80`、`w-model-dev/scripts/lib/parse-phase.ts:4`
- 执行：底稿 Step 1-5（拆分 logic/cli、删原文件、测试改路径、计数同步、全量兜底 grep）。cli 入口结尾用 `runMain(main)`（Task 4 已建）；`process.exit(0)` 改 `process.exitCode = 0`
- **勘误覆盖（E2/E3，优先于底稿 3d/3e 原文）**：
  - 3d `SKILL.md:165` 精确替换对（E3：self-test 单列，不称第 8 个工具）：
    - 旧：`` `scripts/cli/`（33 个 .ts：26 个 check-* 门禁 + 7 个工具 CLI）``
    - 新：`` `scripts/cli/`（34 个 .ts：26 个 check-* 门禁 + 7 个工具 CLI + self-test.ts 回归基线；其中 33 个为 exit-2 结构化错误脚本）``
  - 3e `INSTALL.md:80` 采用底稿 3e 文案（与 E3 一致）：`exit-2 脚本口径 = 26 check + 7 工具 CLI（security-scan / wm-status / metrics-report / ensure-codegraph-opsx / wm-write / doctor / plan-chunks）= 33，self-test.ts 单列（回归基线，非 exit-2）`；目录树注释 plan-chunks 移至 cli/ 行
  - 3a `docs-consistency-logic.ts:22` 注释按底稿原文（26 check + 7 工具（含 cli/plan-chunks）= 33，self-test 不计入）——与 E2 一致，无需改动
  - 口径限定纪律（E2）：任何出现 33/34 的位置必须带「exit-2 脚本」或「文件总数」限定词，禁止裸计数
- 验证（底稿 Step 4）：`__tests__/plan-chunks.test.ts` 全绿；`npx tsx w-model-dev/scripts/cli/plan-chunks.ts w-model-dev/references/phase-1-requirements.md --phase=1 --node-type=REQ` exit 0 出 JSON 分块计划；对不存在路径 exit 2 + ERROR_JSON；`check-docs-consistency.ts` passed:true；`npm run self-test` 256/256
- Commit：`refactor: plan-chunks 拆分 logic/cli 并统一 exit-2 脚本计数口径（P5/P7）`

- [ ] **批 2 末主会话验证门禁**（含批 1 三项 + 以下两项；记录留档）

```bash
# 纯度：logic 层无 fs/exit（gate-logic.ts 豁免除外）——用 Grep 工具
#   pattern: from 'node:fs'|process\.exit   scope: w-model-dev/scripts/logic/
#   期望命中文件仅 gate-logic.ts（若其确有豁免）或零命中
# 端到端：
npx tsx w-model-dev/scripts/cli/plan-chunks.ts w-model-dev/references/phase-1-requirements.md --phase=1 --node-type=REQ   # exit 0
npx tsx w-model-dev/scripts/cli/plan-chunks.ts does-not-exist.md --phase=1 --node-type=REQ                                 # exit 2 + ERROR_JSON
```

---

## 批 3：内容修正（Task 7-11，P1/P11/P6/P14/P8/P9/P12/P13/P15/P16）

### Task 7（P1/P11）：反模式 #48 + #18/#19 详细节 + #22 修正 + 47→48

**子代理任务书**
- 必读：底稿 `### Task 7` 全节（Step 1-9）；现场 `w-model-dev/references/anti-patterns.md`（清单表 127 行 / 详细节 / 检测信号表）、`SKILL.md:82-86`、`docs-consistency-logic.ts:89`、`dispatch-matrix.md`
- 执行：底稿 Step 1-9。#48 追加表尾（D3：编号 #1-47 冻结，不重排）；`#1~#47`→`#1~#48`；maxAntiPattern 47→48；SKILL.md 的 #22 引用全部替换为 #48（底稿 Step 5 标题写 5 处、正文写 6 处——**以现场 grep 实数为准**，Grep pattern `反模式 #22` 范围 SKILL.md，全部替换后复验零命中）
- 勘误覆盖：无
- 验证（底稿 Step 8）：`check-docs-consistency.ts` passed:true；`npm run self-test` 256/256；`grep "47 条"` 于 README/AGENTS/SKILL/references 期望零命中
- Commit：`fix: 新增反模式 #48（子代理越界实施）并修正 SKILL.md #22 误引（P1/P11）`
- 前置：必须在 Task 6 完成后执行（AGENTS §8 plan-chunks 行已就位，计数文案基于文件终态）

### Task 8（P6/P14）：角色矛盾 + 章节引用修正【含 E1 勘误】

**子代理任务书**
- 必读：底稿 `### Task 8` 全节（Step 1-4）；现场 `subagent-delegation.md:1171/:1187`、`command-reference.md:5`、`SKILL.md:83`、`verifier-spec.md` 章节结构（§6 L399 / §7 L535 / §8 L752）
- 执行：底稿 Step 1-4，其中 Step 2 按下方勘误执行
- **勘误覆盖（E1，优先于底稿 Step 2 原文）**：
  - 底稿原文（错误）：`按 verifier-spec.md §7（输出 Schema）+ §8（提示词模板）产出 VerifierOutput JSON`
  - 勘误终态（正确）：`按 verifier-spec.md §6（输出 Schema）+ §8（提示词模板）产出 VerifierOutput JSON`
  - 落点 2 处：`subagent-delegation.md:1187`、`SKILL.md:83`（V 行）。依据：verifier-spec.md 实测 §6=输出 Schema（JSON）、§8=评审提示词模板；§7 是子标准定义
  - Step 3（command-reference.md:5 补 A/R）无勘误，按底稿执行
- 验证（底稿 Step 4 + 勘误增补，全部用 Grep 工具，期望零命中）：
  - pattern `四角色` 范围 `w-model-dev/references/*.md` + `w-model-dev/SKILL.md`
  - pattern `§8 产出 VerifierOutput` 范围同上
  - pattern `§7（输出 Schema）` 范围 `w-model-dev/`（E1 勘误终态校验）
  - `check-docs-consistency.ts` passed:true
- Commit：`docs: 修正角色数量矛盾与 verifier-spec 章节引用（P6/P14）`

### Task 9（P8/P9）：CLI 样板抽取 + artifact-gate 瘦身 + bdd as-any

**子代理任务书**
- 必读：底稿 `### Task 9` 全节（Step 1-9）；现场 `check-iceberg-sweep.ts:200-208`、`check-preventive-review.ts:266-276`、`check-bdd-model.ts:376-384`、`check-artifact-gate.ts:67-123/199-228`、`bdd-logic.ts:989`、`check-budget.ts:152-184`、`check-maturity.ts:132-168`
- 执行：底稿 Step 1-9。新建 `lib/parse-args.ts` + `lib/gate-log-writer.ts` + 两个测试文件；三处 gate-logs 样板替换；`--key=value` 自解析迁移（底稿 Step 4 清单 21 文件，以现场 grep 核实为准）；budget/maturity 降级复用 readJsonlOptional（保留原 warning 文案）；artifact-gate RTM 读取改 readJsonClassified、parsePhaseArg 收敛（先读 lib/parse-phase.ts 确认导出行为，AGENTS.md §3 `--phase=99` 报错样例输出不变）；bdd-logic:989 去 as any
- 勘误覆盖：无
- 验证（底稿 Step 8）：vitest 四个测试文件全绿；`npm run self-test` 256/256；`npx tsx w-model-dev/scripts/cli/check-artifact-gate.ts --phase=99` 得 ✗ [ARG_INVALID] + exit=2（与 README §ERROR_JSON 示例一致）
- Commit：`refactor: 抽取 parse-args/gate-log-writer 样板，瘦身 artifact-gate，去 bdd as-any（P8/P9）`
- 前置：Task 4 已完成（runMain/HandledCliError 就绪）

### Task 10（P12）：schema 自描述补齐

**子代理任务书**
- 必读：底稿 `### Task 10` 全节（Step 1-3）；现场 `w-model-dev/schemas/`（20 份，design-contract 缺 $id，6 份缺顶层 description）
- 执行：底稿 Step 1-3。design-contract 补 `$id` + `description`；budget / coverage / exemption / maturity / verifier-output 5 份补 `description`（文案用底稿 Step 1 给定内容，规则族编号以 AGENTS.md §8 为准核对）
- 勘误覆盖：无
- 验证（底稿 Step 2）：Grep 工具 pattern `"\$id"` 与顶层 description 扫描 20 份全齐（design-contract.schema.json 须出现 `"$id"`）；`check-docs-consistency.ts` passed:true（schema 数仍 20）；`npm run self-test` 256/256
- Commit：`docs: 补齐 design-contract $id 与 6 份 schema 顶层 description（P12）`

### Task 11（P13/P14/P15/P16）：persona 统一 + opsx 日志 + gate-logic 单测 + 导引 + 术语 + CHECKPOINT 清单

**子代理任务书**
- 必读：底稿 `### Task 11` 全节（Step 1-6）；现场 `w-model-dev/subagent/`（product-manager.md:6 + 5 个 hex color 文件，名单在底稿 Step 1）、`ensure-codegraph-opsx.ts:44-116`、`logic/gate-logic.ts` 顶部 export、三个大文件（subagent-delegation / verifier-spec / data-models）
- 执行：底稿 Step 1-6。persona 统一（删 tools 行 + 5 处 hex→命名色，映射表底稿已给）；opsx 6 处 catch 加 stderr 日志（标签按步骤语义命名）；新建 `__tests__/gate-logic.test.ts`（**第一步先读 gate-logic.ts 实际签名与 samples/gate/ fixtures，断言对齐 self-test.ts 同名样本期望退出码**）；三个大文件加 §0 分节导引（按实际章节填表）；glossary 加 2 条（反模式术语 / exit-2 口径——后者与 E2 表述一致：26 check + 7 工具 = 33，self-test 单列）；dispatch-matrix:9 反例→反模式 + 全 references 术语清理；anti-patterns.md H1 更名（先 grep 锚点链接确认无破坏）；command-reference 加 CHECKPOINT 统一清单节（表底稿已给）
- 勘误覆盖：无（底稿 5a 的 exit-2 口径条目与 E2/E3 一致）
- 验证（底稿 Step 6）：`__tests__/gate-logic.test.ts` 全绿；`npm run self-test` 256/256；`check-docs-consistency.ts` passed:true；Grep `color: *"` 与 `^tools:` 于 subagent/ 期望零命中
- Commit：`test+docs: gate-logic 专属单测、persona 统一、opsx 吞错日志、分节导引与术语规范化（P13/P14/P15/P16）`

- [ ] **批 3 末主会话验证门禁**（含批 1 三项 + 勘误终态 grep，全部用 Grep 工具；记录留档）

| Grep pattern | 范围 | 期望 |
|---|---|---|
| `反模式 #22` | `w-model-dev/SKILL.md` | 零命中 |
| `47 条` | README.md / AGENTS.md / `w-model-dev/`（排除 changes/、superpowers/） | 零命中 |
| `§7（输出 Schema）` | `w-model-dev/` | 零命中（E1 生效） |
| `§8 产出 VerifierOutput` | subagent-delegation.md / SKILL.md | 零命中 |
| `四角色` | `w-model-dev/references/*.md` + SKILL.md | 零命中 |
| `color: *"` 与 `^tools:` | `w-model-dev/subagent/` | 零命中 |
| `logic/plan-chunks` | `w-model-dev/` + `docs/`（排除归档/superpowers） | 零命中 |

另跑：`npm run self-test` 256/256；`npx vitest run --config config/vitest.config.ts` 全绿（记录实测 files/tests 数，Task 12 回填用）；`npx tsx w-model-dev/scripts/cli/check-docs-consistency.ts` passed:true

---

## 批 4：发布收尾（Task 12，全量同步）

### Task 12：SSoT 同步 + 41.18.0 发布【含 E1/E2 连带】

**子代理任务书**
- 必读：底稿 `### Task 12` 全节（Step 1-5）；spec §3 E2 口径表（34 文件 / 33 exit-2 / 7 工具 + self-test 单列）作为 SSoT 同步输入
- 执行：底稿 Step 1-5。SSoT 命中处逐个同步（无命中不改）；README/AGENTS/CONTRIBUTING 计数以**批 3 末实测值**回填（vitest files/tests 数）；`__tests__/README.md` coverage 矩阵补 5 个新测试文件行；版本 bump 三处（SKILL.md:3 / skill-metadata.json / CHANGELOG）
- **勘误覆盖（E1 连带）**：底稿 Step 3 CHANGELOG 条目文案中「verifier-spec §7/§8 引用修正」须写为「verifier-spec §6/§8 引用修正」（与 Task 8 实际修复一致）
- **勘误覆盖（E2 纪律）**：CHANGELOG/README/AGENTS 中任何脚本计数必须带口径限定词（「exit-2 脚本 33」或「cli/ 文件 34」），禁止裸计数
- 验证（底稿 Step 4）：`npm run format`；`npm run self-test` 256/256；`npx vitest run --config config/vitest.config.ts` 全绿；**`npm run prepush` 16 项门禁全通过**（Windows 下若纯 PowerShell 环境受限，按 .githooks 说明用 Git Bash 执行并在报告注明）
- Commit：`chore(release): 41.18.0 — 审计十六项修复全量同步（SSoT/README/AGENTS/CHANGELOG）`

- [ ] **批 4 末最终验收（主会话，DoD 双清单）**

底稿 DoD 5 条：
1. self-test / vitest / prepush 全绿
2. `反模式 #22`、`47 条` grep 零残留
3. `logic/plan-chunks.ts` 已删除；`schema-loader.ts` 无 `node:fs`/`process.exit`
4. 版本三处一致 = 41.18.0（SKILL.md frontmatter = skill-metadata.json = CHANGELOG 最新条目）
5. 16 项问题全部有落点

spec 增量 DoD（E1-E3）：
6. `§7（输出 Schema）` 零命中；verifier-spec 引用为 §6+§8（subagent-delegation.md:1187 / SKILL.md:83）
7. 「7 工具 + self-test 单列」口径在 SKILL.md / INSTALL.md / docs-consistency-logic / glossary 四处一致；全库无裸计数 33/34
8. 四批次验证记录齐备（批次号 + 命令 + 输出摘要），已归档供 CHANGELOG 与最终审查引用

---

## 风险与缓解（执行层增量，spec §7 之外）

| 风险 | 缓解 |
|---|---|
| 子代理照抄底稿忽略勘误覆盖（E1-E3） | 每任务书「勘误覆盖」小节全文内联修正内容并声明优先级；批 3/批 4 门禁含勘误终态 grep |
| 底稿行号与现场漂移 | 全局约定 #2：行号仅作定位参考，以内容定位为准，先读现场再改 |
| Task 7 的 #22 处数（底稿 5 vs 6 不一致） | 任务书明确「以现场 grep 实数为准，替换后复验零命中」 |
| 底稿命令含 `/workspace` 与 bash 语法 | 全局约定 #1：路径替换为 REPO；grep 类核对统一改用 Grep 工具执行 |
| prepush 在纯 PowerShell 受限 | 任务书注明 Git Bash 回退；报告记录执行环境 |
| 中间批次验证失败 | 停在该批不进下一批；子代理报告不通过项，主会话决策修复或回退（单一任务 commit 粒度回退） |
