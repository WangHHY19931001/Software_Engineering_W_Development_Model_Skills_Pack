# e2e 基线记录（2026-08-28，三维度优化批次 1）

> Task 1.5（计划：docs/superpowers/plans/2026-08-28-w-model-dev-3dim-optimization.md）。
> demo：todo-rest-demo —— Node ≥20 内置 node:http + node:test，零 npm 依赖内存 Todo REST API（固定规格见 `eval/e2e/demo/SPEC.md`，两次 e2e 逐字共用）。
> 模式：非 self-as-verifier（O 编排 + S/V/G/R 子代理分派，主会话控制器扮演 O）；成熟度 L2（TLA+ L1 + BDD L1 必跑，L2-L4 可选不产出——约束 #13 降载，记录为偏差 D1）。
> 执行环境：worktree `.worktrees/b1-3dim`（branch batch1-3dim）；起始于 a9808ea；demo 位于 `eval/e2e/demo/`（gitignored，计划偏差见 D2）。

## CHECKPOINT 代行声明（账本既有裁定）

e2e 中全部 🔴 CHECKPOINT（项目初始化 / 每阶段进入确认 / ingestion 收敛确认 / 阶段门放行 / 发布放行）的「用户确认」角色由**计划预设通过判据**代行：进入确认=SPEC 冻结 + 前置产物齐全；阶段门放行=该阶段全部门禁 exit 0 且 V qualityLevel ∈ {A,B} 且无 Mandatory 返工项；发布放行=check-artifact-gate 终检 exit 0 + RTM coveragePercent=100 + 四级测试真实通过。门禁与测试全部真实执行，本记录如实标注此代行。

## 总览指标（截至本记录最新更新）

| 阶段 | 状态 | V 评级 | 门禁结论 | 返工 |
| --- | --- | --- | --- | --- |
| 1 需求分析 | 放行 | A / 0.8675 | bdd-model 等 exit 0（gate-logs 4 次） | 0 |
| 2 系统设计 | 放行 | A / 0.9002（重评审） | 全套 5 项 10 次全绿 | 2（verifier-output / tla-bdd-sync） |
| 3 概要设计 | 放行 | A / 0.9044 | 全套四项首轮全绿 | 0（R3 Required 在 V 前消化） |
| 4 详细设计 | 放行 | A（phase4-verifier-output.json） | 全套门禁绿 + R3×3 + preventive exit 0 | 0 |
| 5 编码 | 放行 | A（phase5-verifier-output.json） | code-tla round=2 exit 0 + R3×3（security 1 Required 真实缺陷→S-fix） | 1（code-tla schema 双轨） |
| 6 集成测试 | 放行（含 1 轮 artifact-gate 返工） | A / 0.925（test） | 6 项门禁全绿（verifier/preventive/bdd/tla/sync×6）+ artifact-gate 残留 2 条 cucumber（D9 已知偏差） | 1（artifact-gate --phase=6 → R→V→G→S-fix） |
| 7 系统测试 | 放行（含 R3 Required 闭环 + REQ-001.systemTest 回填） | A / 0.928（test） | 4 项全绿 + artifact-gate 残留 2 条 cucumber（D9） + REQ-001 回填后非 D9 违规清零 | 1（R3 Required×2 → S-fix；artifact-gate 新违规预判复现 → 回填） |
| 8 验收测试 | 完成/发布 | A / 0.916（test） | 终检：verifier/preventive/bdd/tla 全绿 + artifact-gate --phase=8 四级测试 74/74 全过 + RTM 100% + 仅 D9 cucumber 残留 2 条 | 0（R3×3 全 passed） |

## 分阶段记录

### 阶段 1（需求分析）——放行

- 产物：`docs/phase1-requirements/` 9 文档 + `docs/uat-path-mapping.md`（11 UAT）+ `features/L1/` 6 feature + `tla/specs/level1/L1_todo_system.tla/.cfg` + `.w-model/{project,rtm,maturity,tla-manifest,bdd-manifest}.json`。
- 分派：S=1、V=1、G=1（bdd 迭代 4 次 gate-log）、R=0；CHECKPOINT=2（判据代行）；返工=0。
- V 证据：completeness 0.87 / clarity 0.85 / consistency 0.80 / testability 0.94 / traceability 0.93；reworkHints 2 Optional（BDD 缺 Empty+listTodosByStatus 边 → 阶段 2 消化；PUT 请求体字段集 GAP-002 → 阶段 2 ADR-002 固化）。
- 阶段 1 偏差（如实登记，见 D3/D4/D5）。

### 阶段 2（系统设计）——放行

- 产物：`docs/phase2-design/` 8 文档（主模板 + system-architecture/glossary/uml-modeling/traceability-matrix/behavior-spec/discipline-dod/system-test 11 ST）；M-1/M-2/M-3 ↔ SD-HTTP-SERVER/SD-TODO-CONTROLLER/SD-TODO-STORE；ADR-001~004（X-User-Id 缺失 400、PUT 体 {title?,status?}、原生 node:http 选型 5 维评分 4.80、CAP 不适用）。
- 图谱：A-evolve 首建 `.w-model/ingestion/graph.json` 16 节点/64 边；ingestion 收敛 2 轮（第 1 轮 R9×22：设计落点§ 契约=主模板 §3 模块 ID M-xxx + trace matrix 三列正则；S-doc 修复后 converged）。
- R3×3：reliability 报 1 Required（5 feature 头 @transitions 缺新边）→ S-bdd 修复；security 6 条 Optional 登记主模板 §10.1 移交阶段 3。
- V 评审：首轮 A/0.9002 → 终门禁 verifier-output exit 1（O3 EVIDENCE_PATTERN 首定位 2 条不合规 → 罚 0.1 → B）+ check-tla-bdd-sync exit 1（6 对 × 28 违规：sync 转移匹配=大小写敏感严格相等 vs feature camelCase 口径 + 中文 When 首 token）。
- **返工循环（约束 #12）**：R 根因报告 RC-phase2-1-01（V 复审 A/0.9375 + check-rootcause-report/verifier-output 双 exit 0）→ S-fix（6 feature @transitions 事件 PascalCase 化 + 25 行 When/And 双锚点 `When X (X)` 移除中文前缀 + 3 条变量 Given + 4 条不变式锚点 + bdd-manifest event 19 处改名；`.tla` 零改动）→ V 重评审 A/0.9002（evidence 首定位修复）→ G 全套复跑 **5 项 10 次全绿**（verifier-output 0.9002/A、tla-bdd-sync 6/6、bdd-model --phase=2 D1-D8、tla-model SANY+TLC 零违反、requirement-graph converged）。

### 阶段 3（概要设计）——放行

- 产物：`docs/phase3-outline/` 8 文档（interface-contract 4 端点 × 10 字段、8 错误码四元组、IT-001~017 集成测试设计）；INTF-Todo-Create/List/Update/Delete；ADR-005~012 固化阶段 2 §10.1 security 全部 6 项移交（POST 体未知字段忽略、畸形 :id 400、非法 ?status= 400、未匹配路由/方法 400 INVALID_ROUTE、S-01 兜底 500 防泄露、64KB 双检）。
- 图谱：20 节点/88 边（defines=SD→INTF 权威方向 + parent SD-TODO-CONTROLLER→INTF + produces 闭合）；图谱门禁 --phase=3 首轮 exit 0（R11 零违规）。
- R3×3：3 Required（RTM 反向缺登、X-User-Id >128 规则落点、ADR-006/011 覆盖声明失实）+ 9 Optional → S-doc 修复（另主动发现并补第 4 处 RTM 缺口 IT-014→REQ-007）→ G preventive exit 0。
- V 评审：A/0.9044（R3 修复 ≥8 处实测核实；evidence 首定位 5/5 合规）。终门禁四项**首轮全绿**（verifier-output 0.9044/A、tla-model --phase=3、bdd-model --phase=3 D1-D8、sync 6/6）。

### 阶段 4（详细设计）——放行

- 产物：`docs/phase4-detailed/` 8 文档（class-design 8 类方法级、data-model 二级 Map、UT-001~035 单元测试设计）；DD id 8 个（DD-HttpServer/DD-RouteTable/DD-RequestBodyReader/DD-TodoController/DD-ValidationPipeline/DD-ErrorBuilder/DD-TodoStore/DD-IdAllocator）；RTM unitTest 35 条回填。
- 图谱：28 节点/139 边（realizes 27 + parent 8 + produces 16）；图谱门禁 --phase=4 exit 0 零违反（O 只读例外亲测后 G 子代理正式回填 + gate-log 存档）。
- R3×3（`4-{dim}.json`）→ G preventive exit 0 → V 评审 A 级（phase4-verifier-output.json，evidence 首定位合规）→ G 终门禁四绿（verifier-output / tla-model --phase=4 / bdd-model --phase=4 / sync×6）。
- 放行持久化：wm-write exit 0 → status=编码、rtm.currentPhase=4。分派统计补记：S-doc/tla/bdd×1、A-evolve×1、R3×3、V×1。

### 阶段 5（编码）——放行（1 轮返工循环）

- O 路由 /wm coding；遗留裁定 #1 处置：RTM 全 REQ 行 codeModule=`SD-1:src/server.ts,SD-2:src/controller.ts,SD-3:src/store.ts`（数值后缀适配 `^SD-[\d.]+:src/.+` 正则；V 复审 A/0.9055 + G 双绿批准）。
- S-coding 产出 `src/` 4 文件（server/controller/store/index.ts，node:http 零依赖）+ `test/unit/` 7 文件 UT-001~035（真实执行 35/35 pass，coverage 95.73 line / 83.71 branch ≥80 门）。
- **返工循环**：check-code-tla-consistency round=1 exit 1（rtm rows[0].codeModule=null 与 schema `["string","null"]` 双轨，D1 SD→codeModule 前置校验）→ R 根因 RC-phase5-1-01 → S-fix 数字化回填全部 REQ 行 → round=2 exit 0（D1 checked=3 / D2=27 / D3/D4=0 如实记录）。
- R3×3（`5-{dim}.json`）：**security Required 发现 IT-013 超限体 500 真实缺陷**（RequestBodyReader 超限路径经 HttpServer catch 未分流 ValidationError → INTERNAL_ERROR 500 + fail() 先 destroy() 致 ECONNRESET → 违反 ADR-011/NFR-002/IT-013 预期）→ S-fix（onRequest catch 内 `error instanceof ValidationError` 分流 + fail() 改 req.pause()）。复检绿：IT 由阶段 6 承接验证。
- 放行持久化：wm-write exit 0 → status=集成测试、rtm.currentPhase=5。

### 阶段 6（集成测试）——放行（1 轮 artifact-gate 返工循环）

- S-test 产出 `test/integration/` 10 文件 IT-001~017（三模块链路 / 校验穿透 / 用户隔离 / 错误一致性 / 并发 / 性能 / 进程兜底）。
- 真实执行：**17/17 pass**（复验 2026-08-29 连跑 3 次稳定：p95=1.954/2.146/1.968ms、max≈7.5ms、n=1000；串行 10.5s 亦全绿；IT-013 超限体 400 INVALID_BODY 进程不崩溃；IT-017 S-01 兜底 500 message 防泄露逐字 `{error:{code:"INTERNAL_ERROR",message:"内部错误"}}`）。
- **IT-010 偏差（d7 已在阶段 6 闭环）**：node v25.8.1 对重复 X-User-Id 头 join `", "`，数组形态不可经 HTTP seam 构造；S-fix 已修订设计文档 IT-010 步骤 4/预期为运行时契约（重复合法头 → 200 独立分桶，400-array 由单元层 UT-003 兜底），与实测断言一致。
- R3×3（`6-{dim}.json`）：completeness Required×2（IT-010 设计-运行时闭环、执行状态回填）→ S-fix 已闭环（17 条 IT 状态真实回填「通过」+ 变更记录）；reliability 0 Required（连跑 3 次稳定/统计口径正确/随机端口无竞态）；security 0 Required（§10.1 六项移交集成层全断言落点通过）。
- V 评审：**A / 0.925 / passed**（test 五轴：coverage 0.93/correctness 0.94/independence 0.92/clarity 0.90/priority 0.92；V 独立复跑 3 次全绿；R3 Required 闭环逐项核实）。
- G 终门禁：**6 项全绿**（check-verifier-output 0.925/A、check-preventive-review exit 0、check-bdd-model --phase=6 D1-D8、check-tla-model --phase=6 SANY+TLC 零违反、check-tla-bdd-sync ×6 全 0、check-artifact-gate --phase=6 **exit 1 残留 2 条 cucumber 证据缺失**）。
- **返工循环（artifact-gate）**：首轮 exit 1 共 13 条（11×coverageStatus「部分」应为「100%」+ 2×cucumber 证据缺失）→ R 根因 RC-phase6-1-01（A；双线程：类 A=coverageStatus 按阶段字段集判定的标签漂移可修复、类 B=SSoT §10.5.1 阶段 5-8 Cucumber 契约 × CON-001 零依赖 × D1 成熟度 L2 不可满足）→ V 复审 A/0.909 认同 → G 双绿（check-rootcause-report + verifier-output exit 0）→ S-fix（wm-write 11 行 coverageStatus=100%）→ G 复跑：11 条清零、仅剩 2 条 cucumber 残留（D9 已知偏差，见 D9）。
- 🔴 CHECKPOINT 阶段门放行（判据代行 + D9 偏差声明）：全部门禁绿（artifact-gate 残留 2 条为已登记偏差 D9 允许的已知红灯）+ V A 级 + 无 Mandatory 返工项 → 放行。O 持久化：project.status=系统测试、rtm.currentPhase=6（wm-write exit 0）。

### 阶段 7（系统测试）——放行（R3 Required 闭环 + REQ-001 回填）

- S-test 产出 `test/system/todo-system.test.ts`（ST-001~011，seam-A 真实 HTTP）：**11/11 pass**（连跑稳定；ST-010 p95=1.46-1.57ms、median≈1.2-1.3ms、max≈6-7ms、n=1000；testThreshold<100ms 通过 + targetValue<50ms 达成；全量 63 文件并行跑 p95≈2.2ms 仍 40+ 倍余量）。
- **ST-011 安全基线**：OWASP 适用子集（输入校验类型混淆/畸形体/恶意串原样存取不执行/错误泄露扫描/头处理）；注入面「不适用」裁定经 src 实扫成立（无 SQL/HTML 汇点、无 eval）；攻击输入后服务存活。
- **ST-007 步骤 4**：D7 裁定（重复 X-User-Id 头运行时 join(', ')）系统层落地——重复合法头→200 独立分桶、可构造 join 超长→400；设计文档加执行注记。
- R3×3（`7-{dim}.json`）：completeness Required×2（系统层错误码 8/8 处置：INVALID_ID 零触达 + INVALID_ROUTE 未钉码 / ADR-006 非法 status 欠账）→ S-fix 闭环（ST-004 畸形 :id 四形态 400 INVALID_ID、ST-009/ST-011 INVALID_ROUTE 钉码、ST-003 archived/空 status 400 INVALID_STATUS；7/8 code 钉死 + INTERNAL_ERROR seam-A 不可达由 IT-014/017 承接、本层否定式无-5xx 观察）；reliability passed（UAT 跨层串扰 +40-45% 有界非 flake）；security passed。
- V 评审：**A / 0.928 / passed**（test）。
- G 终门禁：verifier/preventive/bdd-model --phase=7/tla-model --phase=7 全绿；artifact-gate --phase=7 首轮 exit 1（2 cucumber D9 + 3 新违规：REQ-001.systemTest 缺失→91%）——3 条为 RC-phase6-1-01 类 A 已预判复现（其 fix 前瞻「阶段 7/8 须回填 REQ-001.systemTest」），O 按阶段 2 traceability-matrix §2「经子需求 ST-001~ST-011 承接」经 wm-write 回填 → G 复跑非 D9 违规清零、仅剩 2 条 cucumber（D9）。
- 🔴 CHECKPOINT 阶段门放行（判据代行 + D9 偏差声明）→ O 持久化：status=验收测试、rtm.currentPhase=7（wm-write exit 0）。

### 阶段 8（验收测试）——完成/发布

- S-test 产出 `test/acceptance/todo-acceptance.test.ts`（UAT-001~011）：**11/11 pass**（2 连跑）+ 全量回归 **74/74**（35 单元 + 17 集成 + 11 系统 + 11 验收）。
- **UAT-008** 验收级性能 p95=1.34-1.50ms（n=1000，testThreshold 通过 + targetValue 达成）；**UAT-010** 零依赖结构断言（package.json 空依赖 + src import 仅 node: 内置 + 相对引用，零裸包名）；**UAT-011** 跨进程重启（POST→kill→同端口重启→GET 200 [] + id 复位 1，无孤儿进程）。
- `docs/uat-path-mapping.md` 11 行实际路径/映射类型按真实测试落点回填（阶段 5 占位清零），与 acceptance 设计、RTM acceptanceTest 列三方一致。
- R3×3（`8-{dim}.json`）：三方全 passed=true、0 Required（completeness 四方一致闭合；reliability UAT-011 连跑 5 次全绿零孤儿、就绪等待 stdout 标记非 sleep；security UAT-006 隔离闭合、零 5xx 攻击路径、新增 FYI=全局 IdAllocator id 间隙可推断他户活动量、冻结 SPEC 未约束）。
- V 评审：**A / 0.916 / passed**（test；验收层「2 码锁码 + 其余分层承接」裁定认同）。
- G 终门禁：verifier/preventive/bdd-model --phase=8/tla-model --phase=8 全绿；**artifact-gate --phase=8 终检**：四级测试 74/74 全过、RTM coveragePercent=100、missingItems=[]、11 行 coverageStatus=100%，仅剩 2 条 cucumber（D9 已知偏差）。
- **归档机制（D11）**：demo 为仓内 gitignored 瞬态评估工作区，技能归档门（check-archive-integrity/check-openspec-archive）面向真实项目 docs/changes/archive/，此处以本 e2e 记录（baseline.md + TSV）作为归档载体，不另建归档目录（见 D11）。
- 🔴 CHECKPOINT 发布放行（判据代行 + D9/D11 偏差声明）：artifact-gate 终检通过（D9 残留 2 条允许）+ V A 级 + 无 Mandatory → 发布。O 持久化：project.status=项目完成、rtm.currentPhase=8（wm-write exit 0）。

## 分派与 CHECKPOINT 计数（累计，8 阶段终值）

- 子代理分派：S=27、A=3、V=11（含阶段 2/5/6 根因复审）、G=16（批量执行约 50 门次）、R=17（R3×3×6 批 + 根因 2）。合计约 74。
- CHECKPOINT：初始化 1 + 每阶段进入确认/放行 10 + 发布放行 1 = 12，全部判据代行（阶段 6/7/8 放行含 D9 偏差声明；阶段 8 另含 D11）。
- 返工：门禁触发 3（阶段 2 verifier-output/sync、阶段 5 code-tla schema、阶段 6 artifact-gate），R3 前置消化 6 批（阶段 2/3/6/7 各 Required 均在 V 前闭环）；根因定位 3 轮（RC-phase2-1-01 / RC-phase5-1-01 / RC-phase6-1-01）。

## 偏差与发现登记（基线如实记录）

- **D1 成熟度降载**：TLA+ L2-L4 / BDD L2-L4 未产出（SPEC 冻结「L1 必跑」+ 约束 #13 L2 可选），各阶段 manifest 以 currentPhase 推进 + L1 资产对齐 SD/INTF 承接。
- **D2 demo 位置**：仓内 gitignored `eval/e2e/demo/` 替代计划原定仓库外目录（写入受限）。
- **D3 阶段 1 基建缺口（不追溯）**：阶段 1 无 ingestion 图、无 R3 报告、闭环 5 脚本中 budget/run-log/checkpoint 三项基建未建立、check-role-dispatch/check-signature-chain 未跑——自阶段 2 起按契约执行，阶段 1 缺口作为基线范围偏差记录。
- **D4 wm-write untyped 通道**：tla-manifest/bdd-manifest/graph/preventive-reviews/verifier-outputs 非 wm-write 注册目标，按 CLI 契约走 `--allow-untyped`（写前 ajv 自验）。
- **D5 技能可观测发现（供批次 2/3 参考）**：① tla-bdd-sync 转移匹配=大小写敏感严格相等，而 bdd-guide §9 仅「名称完全一致」四字指引，与 D4/manifest 的 camelCase 口径存在双轨张力；② check-verifier-output 的 evidence 首定位正则（verifier-logic L236）+ O3 罚分机制对 V 产出构成隐性格式契约，V 自检未覆盖即 exit 1；③ R9 设计落点§ 与 R11/R13 的落点正则逐阶段不同（M- 前缀 vs §数字），跨阶段一致性成本高；④ R3 报告命名 `<phase>-{dim}.json`（非 phaseN-）易误写；⑤ coverageStatus 语义在 rtm-guide/hard-constraints/SKILL.md 为「7 字段全集」口径、gate-logic 按**阶段字段集**判定，无单一事实源与「阶段边界重标注」衔接规则（阶段 6 实际触发 11 条假阳性，D10）；⑥ SSoT §10.5.1 阶段 5-8 Cucumber 执行证据为无条件强制契约且无成熟度分级/零依赖豁免 off-ramp，低成熟度或零依赖项目只能带红灯运行或违反自身约束（D9）。
- **D6 CHECKPOINT 代行**：全部 CHECKPOINT 由计划预设判据代行（见顶部声明）。
- **D7 IT-010 duplicate X-User-Id 设计-运行时偏差（阶段 6）**：设计文档（interface-contract §2.1）声明重复头 → 400 数组形态，但 node:http v25 运行时将重复头 join `", "` 成单串——400-array 形态无法经 HTTP seam 构造；测试断言实际运行时契约（ADR-001/009 仍满足：非法形态 400 INVALID_USER），偏差 inline 记录于测试文件，不构成未登记漂移。
- **D8 阶段 5 RTM codeModule 适配**：RTM 全 REQ 行 codeModule 采用数值后缀 `SD-1/2/3:src/...`（对应 SD-HTTP-SERVER/SD-TODO-CONTROLLER/SD-TODO-STORE），适配 `^SD-[\d.]+:src/.+` 正则；V 复审 + G 双绿批准，属设计-实现映射的表示层适配而非语义改变。
- **D9 artifact-gate 阶段 5-8 Cucumber 执行证据不可满足（阶段 6）**：SSoT §10.5.1 规定阶段 5-8 项目 Artifact Gate 强制 required Cucumber 执行证据（`.w-model/bdd/reports/report.json`，无项目级 opt-out、不得以 L1「额外行为门可选」解释为 fail-open）；demo 冻结 SPEC CON-001（零 npm 依赖，src 仅 node:builtins）∩ 成熟度 L2 降载（D1，仅产出 BDD L1）→ cucumber 运行时无法引入，报告无法诚实产出。O 裁定：作已登记偏差记录（禁止伪造报告），artifact-gate 阶段 6 残留 2 条 cucumber violations 为**允许的已知红灯**；经 R 根因（RC-phase6-1-01，upstreamDefect=技能侧契约）+ V 复审 + G 门禁闭环。
- **D10 阶段 6 artifact-gate coverageStatus 标签漂移（已修复）**：rtm.json 11 行按 rtm-guide/hard-constraints「7 字段全集」口径标「部分」，而 gate-logic 按**阶段字段集**判定（阶段 6 不含 systemTest、NFR/CON 横切行减免）实际 rowComplete=true → 首次跑 artifact-gate 暴露 11 条违规；S-fix 经 wm-write 改标 100% 后清零。根因为 coverageStatus 语义多文档口径无单一事实源（见 D5 ⑥）。
- **D11 归档机制**：demo 为仓内 gitignored 瞬态评估工作区；技能阶段 8 归档门（check-archive-integrity/check-openspec-archive）面向真实项目的 docs/changes/archive/ 受控归档，与 demo 定位不符；本 e2e 以本记录（baseline.md）+ TSV 行作为归档载体，不另建归档目录，记录为基线偏差。另：UAT-011 依赖评估环境 tsx（worktree 根 node_modules）属环境耦合，终值复跑需同布局（R3 FYI）。

## 卡点记录

- 2026-08-29T02:22+08:00：子代理分派撞 5 小时用量上限（03:37 重置）；O 以只读例外亲测图谱门禁 --phase=4 通过；恢复步骤已登记 SDD 账本与 `.w-model/e2e-baseline-notes.md`。非技能卡点。
- 2026-08-29T05:04+08:00：阶段 6 R3×3 三个子代理全部撞每周/每月用量上限 [1310]（重置 2026-09-04 10:16:20），后续重派成功恢复执行。非技能卡点。

## 最终状态（发布完成）

- **四级测试全部真实执行通过**：单元 35/35（coverage 98.22%）、集成 17/17、系统 11/11、验收 11/11 = **74/74**。
- **RTM**：11 行 coverageStatus=100%、coveragePercent=100、missingItems=[]；REQ-001 域根经子需求全承接（systemTest/acceptanceTest 已按阶段 2 矩阵口径回填）。
- **门禁**：各阶段 check-verifier-output（全 A 级）、check-preventive-review、check-bdd-model / check-tla-model / check-tla-bdd-sync、check-code-tla-consistency、check-artifact-gate 均达成阶段放行；唯一常驻红灯 = artifact-gate 阶段 5-8 的 2 条 cucumber 证据缺失（D9 已登记偏差，O 裁定允许、禁伪造）。
- **项目状态**：project.status=项目完成、rtm.currentPhase=8（wm-write exit 0）。

## 技能可观测收获（批次 2/3 输入，D5 汇总）

1. tla-bdd-sync 转移匹配大小写敏感 vs bdd-guide「名称完全一致」四字指引不足（阶段 2 首撞）。
2. check-verifier-output evidence 首定位正则 + O3 罚分是隐性格式契约，V 自检未覆盖即 exit 1（阶段 2/7 各撞一次）。
3. R9/R11/R13 落点正则逐阶段不同（M- 前缀 vs §数字），跨阶段一致性成本高。
4. R3 报告命名 `<phase>-{dim}.json` 易误写为 phaseN-。
5. coverageStatus 多文档口径（7 字段全集 vs 阶段字段集）无单一事实源，触发 D10 假阳性。
6. SSoT §10.5.1 阶段 5-8 Cucumber 执行证据无条件强制、无成熟度/零依赖 off-ramp → 低成熟度/零依赖项目只能带红灯或违反约束（D9）。
7. artifact-gate 自阶段 6 起才被运行，coverageStatus/REQ-001.systemTest 等阶段字段集漂移延后暴露（RC-phase6-1-01 类 A 前瞻验证）。
8. R3/R/V 分层带来真实缺陷拦截：阶段 5 security R3 现场定位 IT-013 超限体 500 缺陷；阶段 6/7/8 各层错误码/安全面/跨进程面闭环。
9. 性能基线充分性：双字段判据（testThreshold 断言/targetValue 记录）在多文件并行串扰下仍稳定（45×/40× 余量），RISK-003 设计成立。

## 收尾（Task 1.5 完成）

- 本记录终值已定；`eval/w-model-dev-results.tsv` 追加 e2e 基线行；提交 `docs(eval): record batch-1 e2e baseline rebuild (todo-rest-demo L2)` → Task 1.6 合并回 main + 批次间用户审查点。
