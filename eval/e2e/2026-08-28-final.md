# e2e 终值记录（2026-08-28 规格，三维度优化批次 3 终值执行）

> Task 3.6（计划：docs/superpowers/plans/2026-08-28-w-model-dev-3dim-optimization.md）。
> demo：todo-rest-demo —— Node ≥20 内置 node:http + node:test，零 npm 依赖内存 Todo REST API（固定规格 `eval/e2e/demo-final/SPEC.md`，与基线 `eval/e2e/demo/SPEC.md` 逐字共用，冻结 2026-08-28）。
> 模式：非 self-as-verifier（O 编排 + S/V/G/R 子代理分派，主会话控制器扮演 O）；成熟度 L2（TLA+ L1 + BDD L1 必跑）。
> **执行引擎：批次 3 重构后的新 SKILL.md（106 非空行，含门禁契约与资源清单）**——本记录与基线记录逐项对比（分派次数 / CHECKPOINT 次数 / 返工次数 / 耗时），衡量易用性重构效果。
> 执行环境：worktree `.worktrees/b3-3dim`（branch batch3-3dim）；起始于 6a2d6bd；demo 位于 `eval/e2e/demo-final/`（gitignored 瞬态工作区，同基线 D2 位置偏差）。

## CHECKPOINT 代行声明（账本既有裁定，与基线一致）

e2e 中全部 🔴 CHECKPOINT（项目初始化 / 每阶段进入确认 / ingestion 收敛确认 / 阶段门放行 / 发布放行）的「用户确认」角色由**计划预设通过判据**代行：进入确认=SPEC 冻结 + 前置产物齐全；阶段门放行=该阶段全部门禁 exit 0 且 V qualityLevel ∈ {A,B} 且无 Mandatory 返工项；发布放行=check-artifact-gate 终检 exit 0 + RTM coveragePercent=100 + 四级测试真实通过。门禁与测试全部真实执行。

## 总览指标

| 阶段 | 状态 | V 评级 | 门禁结论 | 返工 |
| --- | --- | --- | --- | --- |
| 1 需求分析 | 放行 | A / 0.9295（重评审；初评 0.9223） | 首轮 artifact-gate exit 1（tla-bdd-sync）→ R 根因 → V 复审 → G → S-fix 选项 B → V/G 复检 → 6/6 全绿 | 1（R 循环） |
| 2 系统设计 | 放行 | A / 0.8770 | 全绿（含 artifact-gate 0） | 1（R3 Required SD-000 → S-fix） |
| 3 概要设计 | 放行 | A / 0.886 | 全绿 | 1（R3 Required×3 D7 契约互斥 → S-fix） |
| 4 详细设计 | 放行 | A / 0.894 | 全绿 | 1（R3 Required onRequest 两分法 → S-fix） |
| 5 编码实现 | 放行 | A / 0.8757 | 全绿（code-tla round 1 即绿；cucumber 真实通道建立，D9 消除） | 1（R3 Required coverage 溯源 → S-fix） |
| 6 集成测试 | 放行 | A / 0.8942 | 全绿（IT 17/17；RC-phase6-1-01 类 A 未复发） | 1（R3 Required 回填 → S-fix） |
| 7 系统测试 | 放行 | A / 0.9028 | 全绿（ST 11/11） | 0（无 Required） |
| 8 验收测试 | 发布 | A / 0.9057 | 终检 4/4 全绿（artifact-gate --phase=8 0：74/74 + RTM 100%） | 2（UAT-002 裁定 S-fix + 超限体 Required S-fix） |

**终值**：8 阶段 V 全 A（0.9295 / 0.8770 / 0.886 / 0.894 / 0.8757 / 0.8942 / 0.9028 / 0.9057）；四级测试 **74/74**（UT 35 + IT 17 + ST 11 + UAT 11）；返工 8 项（1 完整 R 循环 + 7 R3-Required S-fix）。基线对照的易用性收益：阶段 1 同型技能缺陷（sync 命名契约）较基线**提前一个阶段首撞**（基线段 2 / demo-final 段 1），R 循环 1 次即闭环；D9 cucumber 常驻红灯由真实证据通道**消除**；全部门禁零常驻红灯。相对基线分派 ↓（≈52 vs ≈74，O 侧 G 门禁代跑未计），CHECKPOINT 18（判据代行，与基线一致），详细对比见下文计数节。

## 分派与 CHECKPOINT 计数（与基线对比）

- 子代理分派（累计，O 侧 G 代跑不计）：阶段 1≈11（S+R3×3+S-fix×2+V×2+R+Vrc+S-fixB）、2-6 各 6（S+R3×3+S-fix+V）、7=5（S+R3×3+V，无 S-fix）、8≈7（S+R3×3+S-fix×2+V）→ **≈52**；基线 ≈74（S=27/A=3/V=11/G=16/R=17）。下降主因：无 ingestion A 子代理阶段（图表直接由 S 产出建图）+ G 门禁 O 侧代跑（模态偏差）+ 阶段 7 零 Required。
- CHECKPOINT：**18**（项目初始化 + 8×进入确认 + 8×阶段门 + 发布，全部计划预设判据代行；与基线同裁定）。
- 返工：**8 项**（阶段 1 完整 R 循环 1 + R3-Required S-fix 7；基线 R 循环 3 + 各阶段 R3 Required 处置）。
- 特殊偏差：阶段 8 UAT-002 过度收紧（O 裁定规格权威，同 D7 谱系）+ 超限体 ECONNRESET（R3 security Required，src/ 修复 + 全量回归 + ≥1MB 用例防回潮）。

## 分阶段记录

### 阶段 1（需求分析）

- 🔴 CHECKPOINT 项目初始化：判据代行通过（SPEC 冻结 + 前置产物齐全：SPEC.md / package.json 基线就位）。
- S 产出：9 份需求分析产物 + 11 条 UAT 验收设计 + 6 个 BDD L1 features + TLA+ L1 模型 + `.w-model` 状态（project/rtm/maturity/bdd-manifest/tla-manifest/ingestion graph）。
- R3 预防性审查（O→R，3 报告）：**2 个 Required 缺陷 + 若干 Optional**：
  1. completeness Required#1：traceability-matrix.md 缺 CON-001/CON-002 行（9/11 行不一致，FM-3D-08）。
  2. completeness Required#2 + reliability Required#1：acceptance-test-design.md UAT-004/006 步骤引用规格外端点 `GET /todos/:id`（SPEC R2 仅列表）。
- S-fix（O→S 兼任 F）：① UAT-004 步 3 改 `GET /todos` 规格内列表断言；UAT-006 步 3 拆为列表不含断言 + PUT→404（R3 规格内）；② traceability-matrix §1/§2 补 CON-001/CON-002 行（11/11 一致）；③ R3 报告 Required finding 曾临时追加 `resolution` 字段作闭环标记，**触发 preventive-review schema（additionalProperties:false）冲突已还原**——闭环标记改存本账本，R3 报告保持 schema 合规。
- 验证：`check-requirement-graph --phase=1` exit 0、`check-bdd-model --phase=1` exit 0、`check-tla-model --phase=1` exit 0（真实 TLC）、`check-preventive-review --phase=1 --variant=standard` exit 0；grep 确认 commit 无 `GET /todos/:id` 残留（剩余 `/todos/:id` 均为 PUT/DELETE 合法上下文）；修改后 .json 均通过 JSON.parse。
- V 评审：O→V 分派完成——phase1.json **A / compositeScore 0.9223 / passed**（五轴 completeness 0.9433 / clarity 0.9133 / consistency 0.8833 / testability 0.9267 / traceability 0.9533；R3 两点独立复核闭合；无 Mandatory；Recommended 2：discipline-dod §2 九项未勾选、UAT-009 步4 400-or-413 张力待阶段 3 ADR；Nit：SD-000 占位待阶段 2 回填）。
- **G 门禁（O 侧代跑，模态偏差已记账）**：6 项中 5 项绿（check-verifier-output 0、check-tla-model --phase=1 0、check-bdd-model --phase=1 0、check-requirement-graph --phase=1 0、check-maturity（正确调用 `<maturity.json> --project=`）0）；**check-artifact-gate --phase=1 exit 1（唯一红灯）** = 独立 TLA↔BDD 文件同步契约 check-tla-bdd-sync（阶段 1-4 无条件启用；bdd-model D4 带 `--require-tla-equivalence` 已过，仅挂文本级 token 严格匹配）。
  - sync violations 3 类：TLA+ 转移 AcceptRequest/Respond*×9 无对应 When；BDD createTodo/createTodoInvalid/用户执行 不在 TLA+ Next；TLA+ 状态 token 16 个（4 真实变量 state/requestKind/activeUser/todoCount + 12 个 VARIABLES 行内注释噪声词 L1/Processing/Succeeded/none/create/list/update/delete/X/User/R5/CON）；不变式 TypeInvariant/BusinessInvariant 无对应 Then。
- **R 根因（约束 #12 返工循环 R 段）**：R 子代理产出 `rootcause-reports/rc-phase1-1.json`——根因链 5-Why：S 协议级 Accept/Respond 建模 vs features 语义事件不同构（主因=新指南 bdd.md §9/tla-plus.md §15 sync 契约仅「名称完全一致」3 行表无工作示例，基线 D5① 同型问题批次 3 未修 = **skill-side upstreamDefect present**；次因=S 执行选择 + 流程缺前置 sync 自检 + V 清单无命名级检查）；唯一推荐=**选项 B**（TLA+ 改语义事件命名与 feature 同构，基线已验证 6/6 绿；不损失验证力——todoCount 越界/用户隔离/错误体一致性不变量可保留）；check-rootcause-report exit 0（A 级）。
- ⏸ **会话中断**：R 报告的 V 复审分派被取消——下一会话从「重派 V 复审 rc-phase1-1.json → G 双门禁 → S-fix 选项 B → V/G 复检 → 阶段 1 放行」恢复（完整恢复点见账本 progress.md「Task 3.6: in progress」段）。

### 阶段 5（编码）— 单元测试覆盖率口径登记

- 🔴 CHECKPOINT 阶段门放行前，R3 reliability 唯一 Required：`rtm.executionSummary.unitTest.coverage=97.67` 需可溯源（R3 侧另测 95.45，疑似口径/范围差异）。
- **口径判定**：`rtm.json executionSummary.unitTest.coverage` 取 **`all files` 行 `line %`**，实测命令 `node --import tsx --test --experimental-test-coverage "test/unit/**/*.test.ts"`（demo-final 根）。O 侧独立复跑输出 tests 35/35 pass、coverage all files **line 97.67 / branch 84.78 / funcs 89.41**（分文件 line：controller 96.69 / server 98.45 / store 98.27）——与 rtm 记录值 97.67 完全一致、可复现。
- **97.67 vs 95.45 差异成因**：`97.67` = node:test 内建覆盖率 `all files` 聚合行的 **line %**（源码行加权汇总，uncovered 行为 store 断言/拆段等，见分文件表）；`95.45` 非 `all files line %` 输出值，属口径差异（如取 branch/取不同 glob 范围或对分文件 line% 做另行加权）。以 O 侧同命令 `all files line % = 97.67` 为准登记于 RTM。
- **schema 约束**：`w-model-dev/schemas/rtm.schema.json` 对 `executionSummary.unitTest` 设 `additionalProperties:false`，禁止在 testSummary 内新增 `coverageNote` 字段（会破坏 schema）。故口径说明**记于本账本**（需求 ②），不污染 RTM 结构；`coverage` 保持真实可复现值 `97.67`（= 最新实测 all files line %）。
- S-fix 复跑确认：覆盖率连跑稳定 `all files line 97.67`，rtm 数字与最新实测一致，无需改值。
- 验证：`check-artifact-gate --phase=5` exit 0、`check-tla-model --phase=5 --graph` exit 0、`check-preventive-review --phase=5 --variant=standard` exit 0。

### 阶段 6（集成测试）— 集成测试覆盖率口径登记

- 🔴 阶段 6 门禁通过后 R3 completeness 唯一 Required（登记性缺口，非行为缺陷）：`rtm.executionSummary.integrationTest.coverage=98.02` 需可溯源（R3 侧另测 98.83，疑似口径/范围差异）。
- **口径判定**：`rtm.json executionSummary.integrationTest.coverage` 取 **`all files` 行 `line %`**，实测命令 `node --import tsx --test --experimental-test-coverage "test/integration/**/*.test.ts"`（demo-final 根）。O 侧独立复跑输出 tests 17/17 pass、coverage all files **line 98.02 / branch 86.98 / funcs 96.43**——与 rtm 记录值 98.02 完全一致、可复现（同阶段 5 处置先例）。
- **98.02 vs 98.83 差异成因**：`98.02` = node:test 内建覆盖率 `all files` 聚合行的 **line %**（源码行加权汇总）；`98.83` 非 `all files line %` 输出值，属口径差异（如取 branch/取不同 glob 范围或对分文件 line% 做另行加权——R3 侧实测 branch=86.77/funcs=96.30 亦与 all files 聚合行 86.98/96.43 偏离，佐证不同统计范围）。以 O 侧同命令 `all files line % = 98.02` 为准登记于 RTM。
- **schema 约束**：`w-model-dev/schemas/rtm.schema.json` 对 `executionSummary.integrationTest` 设 `additionalProperties:false`，禁止在 testSummary 内新增 `coverageNote` 字段（会破坏 schema）。故口径说明**记于本账本**，不污染 RTM 结构；`coverage` 保持真实可复现值 `98.02`（= 最新实测 all files line %）。
- **R3 completeness Required#1（设计文档执行状态未回填）闭环**：`docs/phase3-outline/todo-rest-integration-test.md` IT-001~017 全部 17 条「执行状态」已由真实运行器回填「通过」（17/17 pass、0 fail；汇总表状态列同步「通过」，变更记录补 S-fix 回填说明），与 RTM integrationTest 17/17/0/0 及实测三载体一致。
- 验证：`check-artifact-gate --phase=6` exit 0、`check-tla-model --phase=6 --graph` exit 0、`check-preventive-review --phase=6 --variant=standard` exit 0。

### 阶段 7（系统测试）— 系统测试覆盖率口径登记

- ✅ 阶段 7 系统测试真实执行 + RTM 回填（S-test 子代理）。
- **执行**：`node --import tsx --test test/system/todo-system.test.ts`（demo-final 根，seam-A 真实起服 + 真实 HTTP）。**两次连跑 11/11 pass、0 fail、0 pending，结果稳定**（node v25.8.1）。ST-010 性能指标两次实测：p95=1.834 / 1.881ms、p99=2.149 / 2.159ms、median=1.434 / 1.480ms、max=6.420 / 5.760ms、n=1000——testThreshold（≤200ms 断言判据）双跑通过、targetValue（<50ms 生产目标）达成。ST-011 安全基线适用子集三面（输入校验 / 错误泄露 / 头处理）通过，SQL/HTML 注入面不适用裁定（进程内 Map 数据层、JSON 响应、无 eval，无 SQL/HTML 汇点）维持原裁定说明。
- **口径登记**：`rtm.json executionSummary.systemTest.coverage` 取 **`all files` 行 `line %` = 96.85**，实测命令 `node --import tsx --test --experimental-test-coverage test/system/todo-system.test.ts`（demo-final 根；分文件 line：controller 99.17 / server 93.48 / store 98.27；branch 85.56 / funcs 94.05）。沿用阶段 5/6 同口径先例（unit 97.67 / IT 98.02）。
- **schema 约束**：`rtm.schema.json` 对 `executionSummary.systemTest` 设 `additionalProperties:false`，禁止 testSummary 内新增口径注记字段；coverage 口径说明记于本账本，RTM 保持 schema 合规（同 phase5/6 处置）。
- **RTM 回填**：`executionSummary.systemTest={total 11, passed 11, failed 0, pending 0, coverage 96.85}`；行级 `systemTest` 按 traceability-matrix §2 承接回填 REQ-001（经 ST-001~ST-011）/ REQ-002~REQ-007 / NFR-001 / NFR-002（CON-001/CON-002 矩阵声明无 ST 用例，保持 null）；`docs/phase2-design/todo-rest-system-test.md` 11 条「执行状态」回填「通过」+ 汇总表状态列同步 + 变更记录登记。实现核验偏差（文档头错误码枚举信息行基线遗留 `RESOURCE_NOT_FOUND` 名 vs impl `NOT_FOUND`；ST-004/ST-009 为 R3 Required 如实超集）已登记于该文档变更记录，不影响用例定义。
- 终确认：回填后 rerun `node --import tsx --test test/system/todo-system.test.ts` 11/11 pass。
- **R3×3 预防性审查（7-{completeness,reliability,security}.json）**：三维均 **passed=true、0 Required**（无需 S-fix）。Optional×3：completeness=ST-004/ST-009 设计步骤表未随执行期闭环同步（如实超集已登记）；reliability=全套件无超时兜底（5 连跑不触发，建议 `--test-timeout`）；security=超限体根治路径对大体量客户端 ECONNRESET（>64KB 形态 ST-011 未覆盖，demo 语境 127.0.0.1 无放大不阻塞，生产化前升 Required）。Nit/FYI 若干：素质文档头「待执行」陈旧文本、错误码枚举行基线遗留 `RESOURCE_NOT_FOUND` 名、D7 join 归因、ST-010 median 取位、泄露扫描非字面穷举、CRLF 无回归锁等——全部非阻塞。
- **G preventive**：`check-preventive-review --phase=7 --variant=standard` exit 0。
- **V(phase7)**：phase7.json **A / compositeScore 0.9028 / passed**（coverage 0.9433 / correctness 0.9233 / independence 0.8833 / clarity 0.8533 / priority 0.8433）；独立复跑 11/11 + coverage 96.85 逐位一致；D7 数组形态与阶段 6 同裁定闭环；基线三项 Required（INVALID_ID/INVALID_ROUTE/ADR-006）独立读码确认闭环无遗漏。
- **G 终门禁（4/4 全绿）**：check-verifier-output 0（A）；check-tla-model --phase=7 --graph 0；check-bdd-model --phase=7 --graph --require-cucumber-report 0；**check-artifact-gate --phase=7 0**——REQ-001.systemTest 前瞻回填生效（RC-phase6-1-01 类 A 未复现）、D9 cucumber 已知红灯经 demo-final 真实报告通道消除（正向偏差，未复发）。
- 🔴 CHECKPOINT 阶段门放行（判据代行：全门禁 exit 0 + V A 级 + 无 Mandatory）→ O 持久化：`wm-write` project.status=**验收测试**、rtm.currentPhase=**8**（exit 0，ok:true）。

### 阶段 8（验收测试）— 验收测试真实执行 + 覆盖率口径登记

- ✅ 阶段 8 验收测试真实执行 + RTM 回填（S-test 子代理）。
- **实现**：`test/acceptance/` 下 3 个独立测试文件承接 UAT-001~011（uat-crud / uat-spec-conformance / uat-perf-constraints），逐一对照 acceptance-test-design.md 步骤表；seam-A 真实起服 + 真实 HTTP（复用 test/integration/helpers/http.ts），零新建测试专用 seam；UAT-011 结构面读 package.json + 扫描 `src/*.ts` import，语义面 child_process 跨进程重启（node --import tsx src/index.ts，PORT 注入随机空闲端口，kill+exit 等待+taskkill 兜底防孤儿）。
- **执行命令**：`node --import tsx --test "test/acceptance/**/*.test.ts"`（demo-final 根）。备注：裸目录形 `node --import tsx --test test/acceptance/` 在当前运行时触发 tsx loader `ERR_UNSUPPORTED_DIR_IMPORT`（目录被当作 ESM 模块导入），等价 glob 形可正常发现全部 11 用例，故采用 glob 形（结果与显式文件列表形逐条一致）。
- **结果**：**三次连跑 10 pass / 1 fail，稳定**（node v25.8.1；UAT-002 失败，其余 10 条全过、0 cancellation、0 skip）。UAT-010 性能两次实测：GET p95=2.122 / 1.764ms、POST p95=2.116 / 1.800ms——testThreshold（≤200ms 断言判据）双跑通过、targetValue（<50ms 生产目标）达成。
- **UAT-002 失败（冻结契约冲突，如实执行不越权）**：设计步骤 3「仅空白 title `{"title":"  "}` → 400」实测 **201**。冻结 SPEC（R1「title 必填 1-200 字符」）+ 冻结接口契约（ADR-005：非法 = 缺失/非字符串/0 或 >200 码点，不含空白）+ `src/controller.ts` validateTitleValue（string 且 1-200 码点，无 trim）+ 真实探针（`"  "`/`"  x  "`/`" \t "` 均 201 原样存储）四处一致判仅空白为合法码点字符串；同 demo 其余全部设计文档均不声明空白非法。本案系阶段 1 验收设计对冻结规格的**过度收紧**（唯一声明点），步骤 5「无副作用列表」断言同步受影响。S 按设计如实执行、未越权改用例定义、未改 src/；建议 O 按 D7 先例裁定（冻结 SPEC + 运行时契约为权威）后经 R→V→G→S-fix 闭环（S-fix 方向：UAT-002 步骤 3 改按规格契约断言 201 + 原样存储并登记偏差）。
- **口径登记**：`rtm.json executionSummary.acceptanceTest.coverage` 取 **`all files` 行 `line %` = 97.67**，实测命令 `node --import tsx --test --experimental-test-coverage "test/acceptance/**/*.test.ts"`（demo-final 根；branch 81.98 / funcs 95.24）。沿用阶段 5/6/7 同口径先例。schema 约束：`rtm.schema.json` 对 testSummary `additionalProperties:false`，禁止新增口径注记字段，口径说明记于本账本（同 phase5/6/7 处置）。
- **RTM 回填**：`executionSummary.acceptanceTest={total 11, passed 10, failed 1, pending 0, coverage 97.67}`（wm-write exit 0，ok:true，schema 校验通过含 count-invariant）；行级 `acceptanceTest` 沿用阶段 1 设计登记（REQ-001~007 / NFR-001/002；CON-001/CON-002 均 UAT-011，status 枚举 pending/completed 与实现一致）；`docs/uat-path-mapping.md` 阶段 8 核对：实际路径列与真实执行一致（UAT-011=结构断言+进程重启 GET /todos），映射类型全保持 `直接`，补「阶段 8 验收执行」节；`docs/phase1-requirements/acceptance-test-design.md` 11 条执行状态回填（10 通过 / 1 失败含失败原因）+ 用例汇总表状态列 + 变更记录。UAT-002 失败状态如实登记，未以「通过」代填。
- 终确认：回填后 rerun 结果稳定（10 pass / 1 fail，与回填一致）。
- **待 O 决策项**：UAT-002 冻结契约冲突的 D7 先例裁定 + R 返工闭环（见上）；该闭环完成前 `check-artifact-gate --phase=8` 将因四级测试含 1 fail 无法全绿，属预期（如实反映验收未全通过）。

### 阶段 8（验收测试）— 收口中

- **S-test：UAT-001~011 真实实现+执行**。`test/acceptance/` 3 文件（uat-crud / uat-spec-conformance / uat-perf-constraints），seam-A 真实起服 + 真实 HTTP。**三次连跑稳定：10 pass / 1 fail / 0 skip**（UAT-002 唯一失败）。UAT-010 性能三份实测 GET p95=2.122/1.764/1.839ms、POST p95=2.116/1.800/1.812ms——testThreshold ≤200ms 通过、targetValue <50ms 达成。UAT-011 双面（CON-001 结构面=零依赖/import 扫描；CON-002 语义面=child_process 真实重启复位）通过。
- **UAT-002 失败（如实，非实现缺陷）**：步骤 3「仅空白 title → 400」实测 **201 原样存储**。五源一致判空白合法：冻结 SPEC R1（title 必填 1-200 字符，字面满足）+ ADR-005（非法=缺失/非字符串/0/>200 码点，**不含空白**）+ src/controller `validateTitleValue`（无 trim）+ 真实探针（`"  "`/`"  x  "`/`" \t "` 均 201）+ demo 其余全部设计文档（唯一声明空白非法的文件即本验收设计）。属**阶段 1 验收设计对冻结规格的过度收紧**。
- **O 裁定（同 demo D7 先例：冻结 SPEC + 运行时契约为权威）**：UAT-002 步骤 3 应改为断言 201 + 原样存储；步骤 5「无副作用列表」相应改为「列表含该条（原样存储验证）」。作为本 demo 已登记偏差（同 IT-010/ST-007 同一裁定谱系）记录。
- 待执行：S-fix（UAT-002 设计步骤 + 测试断言对齐 + rerun 11/11 + 回填 rtm passed=11）→ R3×3（8-{dim}.json）→ G preventive → V(phase8) → G 终门禁（artifact-gate --phase=8 终检）→ 发布放行。

### 阶段 8 S-fix — R3-security 8-security Required ①（超限体大客户端 ECONNRESET 假安全）返工闭环

- **触发**：`.w-model/preventive-reviews/8-security.json` Required finding 1——「>64KB 超限体经大体量客户端（实测 1MB）读到 ECONNRESET 而非 400 INVALID_BODY」在验收线零可见性（UAT-009 步骤 4a 仅 ~65KB 边缘量级），阶段 7 记录 Optional 并明示「生产化暴露前应升 Required」，阶段 8 最终验收门正式升 Required。
- **根因与修复（src/server.ts 单文件，拒绝路径行为）**：旧实现超限/畸形体拒绝仅 `req.pause()`，不排空残余请求帧、不设 Connection: close，回写 400 后连接被 destroy，大客户端读到 ECONNRESET 零错误体。修复：`RequestBodyReader.fail()` 移除 `req.pause()`；`HttpServer.onRequest` catch 对 `!req.complete`（体未完整接收）单点先 `req.resume()` 排空至请求完整（新增 `drainUntilIncomingEnd`），再回写 400 + `Connection: close`。**顺序关键**：node 对 Connection: close 响应在写完后 destroySoon（`socket.end()` + `finish→destroy`），若客户端仍在发送大体量体且接收缓冲未清空，内核 RST→ECONNRESET；先等排空完成使 socket 无未读数据，断开即优雅 FIN，任意体量下客户端必读到完整 `400 {error:{code:"INVALID_BODY",message}}`。`req.complete===true` 路径（合法体校验错误/路由错误/500 兜底）零变更，keep-alive 语义不变；ErrorBuilder/错误码清单未动（与 ERROR_REGISTRY 一致）。
- **codegraph 前置**（约束 #14）：`codegraph_explore` 会话不可用（demo-final 无 `.codegraph/` 索引），如实落盘 `.w-model/codegraph-queries/phase8-sfix-oversize.md` 不可用性声明 + 等价人工影响分析（同 phase5/6/7 declaration 先例），未伪造 blast radius。
- **验收用例扩展**：`test/acceptance/uat-spec-conformance.test.ts` UAT-009 步骤 4a 新增 **≥1MB** 超限体断言（1MB node:http 客户端，断言 status=400 + assertErrorBody INVALID_BODY + assertErrorStructure + assertNoLeak，非 ECONNRESET/非 status 0），与 ~65KB 边缘断言并存；同步登记 `docs/phase1-requirements/acceptance-test-design.md` UAT-009 步骤 4a 步骤表行 + 变更记录。实测探针（node v25.8.1，修复后）：~65KB/1MB/5MB/1MB-chunked（node:http 客户端）与 5MB/20MB/64MB（raw net 客户端）全部读到 400 + INVALID_BODY 完整错误体、优雅断开（hadErr=false）；修复前 1MB/5MB → ECONNRESET、status=0、零错误体。拒绝后正常 POST 仍 201（进程存活）。
- **四层全量回归（demo-final 根，真实执行）**：UT `node --import tsx --test "test/unit/**/*.test.ts"` **35/35 pass 0 fail**；IT `node --import tsx --test "test/integration/**/*.test.ts"` **17/17 pass 0 fail**；ST `node --import tsx --test test/system/todo-system.test.ts` **11/11 pass 0 fail**（ST-010 p95=1.876/1.834ms，testThreshold ≤200ms 通过）；UAT `node --import tsx --test "test/acceptance/**/*.test.ts"` **11/11 pass 0 fail**（UAT-010 GET p95=2.120ms、POST p95=2.268ms；含 UAT-002 S-fix 后全绿 + 新增 ≥1MB 断言）。性能指标均较改动前基线（GET 2.12/2.126ms、POST 2.27/2.116ms 量级）无升高，testThreshold 判据下无回归（改动仅触及拒绝路径，性能采样路径均为合法 200/201 请求，不受影响）。
- **RTM 覆盖率回填（S-fix 改 src/，coverage 数值变化，wm-write 口径更新）**：node:test 内建 `--experimental-test-coverage` all files line % 复测——unit **97.67→97.56**、integration **98.02→98.00**、system **96.85→95.45**（src/server.ts 新增整流排空代码行在 ST 面未全部命中）、acceptance **97.67→97.67**（不变）。`executionSummary` 四层 coverage 经 `wm-write ".w-model/rtm.json" --from <tmp> --lock-timeout 10000` 回填（exit 0 ok:true，schema additionalProperties:false 未加字段），lastUpdated 刷新；pass/failed/pending 计数不变（35/17/11/11 全 0 fail）。R3 侧 8-security 报告、G preventive、V(phase8)、artifact-gate --phase=8 终检为其后独立门禁步骤，不属本 S-fix 记录范围。

### 阶段 8（验收测试）— 门禁链与发布放行

- **R3×3（8-{dim}.json）**：completeness **passed**（0 Required；Optional：UAT-009 步骤 4「400 或 413」开放择一未钉死文档级漂移；独立复跑 11/11 + line 97.67 逐位一致；src/ mtime 实证 UAT-002 S-fix 未触代码）；reliability **passed**（0 Required；Optional×2：UAT-011 端口 TOCTOU、无超时兜底；4 连跑稳定无 flake、97.67 可复现）；**security passed=false（1 Required）**=超限体大客户端 ECONNRESET（阶段 7 预警升格）→ 已 S-fix（src/server.ts 排空+Connection:close + UAT-009 ≥1MB 断言 + 四层回归全绿 + 覆盖重测回填 unit 97.56/IT 98.00/ST 95.45/acceptance 97.67）。G preventive exit 0。
- **V(phase8)**：phase8.json **A / compositeScore 0.9057 / passed**（coverage 0.9433 / correctness 0.9167 / independence 0.8933 / clarity 0.8567 / priority 0.8633）；Required 闭合**独立实测**（1MB/5MB/20MB/64MB 全读 400 + INVALID_BODY、优雅 FIN）+ 复现 11/11 + line 97.67；R3 Optional 均判可放行后处理。
- **G 终门禁（4/4 全绿）**：check-verifier-output 0（A）；check-tla-model --phase=8 --graph 0；check-bdd-model --phase=8 --graph --require-cucumber-report 0；**check-artifact-gate --phase=8 终检 0**（四级测试 UT35+IT17+ST11+UAT11=74/74 全过、RTM coveragePercent=100、无 D9 残留、无 Mandatory）。
- 🔴 **CHECKPOINT 发布放行（判据代行：artifact-gate 终检 0 + RTM 100% + 四级测试真实通过 + V A 级 + 无 Mandatory）→ O 持久化**：`wm-write` project.status=**项目完成**、rtm.currentPhase=8（exit 0，ok:true，bak 归档）。
- **e2e 终值（对照基线）**：8 阶段 V 全 A；74/74 四级测试；返工 8 项（1 R 循环 + 7 R3-Required S-fix）；分派 ≈52（vs 基线 ≈74）；CHECKPOINT 18（判据代行）；D9 cucumber 常驻红灯消除（真实报告通道）；零常驻红灯收尾。批次 3 易用性重构收益量化记录完成。
- 归档偏差：demo-final 按 demo 惯例不做 full archives（与基线 D11 同裁），e2e 证据以本记录 + rtm/verifier-outputs/gate 证据链留存。
