# 集成测试文档

> 阶段 6（集成测试执行）产出。套用时替换 `{{}}` 占位符。
>
> 集成测试聚焦模块间交互正确性：接口契约、参数校验、跨模块数据传递。测试用例源自阶段 3（概要设计）的集成测试设计，详见 [phase-6-integration-test.md](../references/phase-6-integration-test.md)。

## 文档信息

- 项目名称：{{项目名称}}
- 文档版本：{{v1.0}}
- 执行日期：{{YYYY-MM-DD}}
- 执行者：{{执行者（S 子代理）}}

## 0. 文档定位与 SSOT 头

> **文档版本**：{{v1.0}}（{{YYYY-MM-DD}} 首版）
> **SSOT 声明**：本集成测试文档 + `.w-model/rtm.json` 的集成测试列为阶段 6 的唯一事实来源；接口契约以 [templates/interface-design/interface-contract.md](./interface-design/interface-contract.md) 为准。
> **DoD 引用**：阶段 6 完成度按 [definition-of-done.md](../references/definition-of-done.md) 七维度标准 + [phase-6-integration-test.md](../references/phase-6-integration-test.md)「验收标准」判定；放行前逐项勾选。
> **自身校验**：`check-artifact-gate.ts --phase=6` 校验集成测试列回填与门禁放行；`check-bdd-model.ts --phase=6` 校验 L3 features（如启用）。
> **禁止占位词**：TBD/TODO/undefined 不得进入正式交付。

## 1. 测试范围

{{集成测试范围：模块间交互、接口契约、参数校验、跨模块数据传递、性能/兼容性/容错等。}}

## 2. 测试用例

| 用例 ID | 测试场景 | 输入 | 预期输出 | 优先级 | 状态 |
|---|---|---|---|---|---|
| IT-001 | 接口调用验证 | 合法请求参数 | 返回预期结果，状态码 200 | 高 | ✅ 通过 |
| IT-002 | 接口参数校验 | 非法参数 | 返回错误信息，状态码 400 | 高 | ✅ 通过 |
| IT-003 | 模块间数据传递 | 跨模块调用 | 数据正确传递和处理 | 高 | ✅ 通过 |
| IT-004 | 接口性能测试 | 高并发请求（100 并发，持续 30s） | P95 响应 < 500ms | 中 | {{}} |
| IT-005 | 接口兼容性 | v1 / v2 接口对照请求 | 向后兼容或返回版本升级提示 | 中 | {{}} |
| IT-006 | 分布式事务补偿路径 | TCC 确认/取消、SAGA 正向/反向恢复 | 补偿执行后状态收敛（幂等），无脏数据残留 | 高 | {{}} |
| IT-007 | 容错/故障注入 | 下游超时/熔断/重试终止条件 | 断路器 OPEN→HALF OPEN 探测放行；重试达上限停止 | 高 | {{}} |

## 3. 接口性能结果

| 指标 | 目标 | 实测 | 是否达标 |
|---|---|---|---|
| P95 响应时间 | < 500ms | {{}} | ✅/❌ |
| 采样请求数 | ≥ 1000 | {{}} | ✅/❌ |

## 4. 失败用例分析

| 用例 ID | 失败现象 | 根因 | 关联模块 | 修复后重跑 |
|---|---|---|---|---|
| {{}} | {{}} | {{}} | {{}} | {{}} |

## 5. 校验

- `check-artifact-gate.ts --phase=6`：集成测试列回填校验，退出码 0
- `check-bdd-model.ts --phase=6 --cucumber-report=<report.json>`（L3 features 时）：退出码 0
- 门禁脚本 stdout 末尾 5 行须贴出作为放行证据（约束 #9）

## 6. 结论

- [ ] 所有接口调用验证通过
- [ ] 参数校验逻辑正确
- [ ] 模块间数据传递无误
- [ ] 接口性能满足要求（P95 < 500ms，采样 ≥ 1000 请求）
- [ ] 失败用例已定位根因并回归
- [ ] 阶段门放行前向用户展示：IT-001~007 执行结果 / P95 响应时间 / 失败用例根因（若有）
